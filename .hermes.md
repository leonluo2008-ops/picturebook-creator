# picturebook-creator 项目工作规范

> 发现机制：Hermes 从 CWD 向上搜索 `.hermes.md`，git root 停止。本文件生效于本仓全部工作。

## 项目事实源（2026-09-19 定版）

- **排产/内容中控台 = Notion《绘本排产中控台》**（唯一事实源）：排产台账 DB + 单册六段页面。用户点选确认，Agent 领料/交付回写。
- **本仓 = 创作规则与工具链唯一事实源**：SKILL.md 规则、references 锚文件、bin 工具。两边用「排产号」关联，互不复制内容。
- CSV 台账 = 一次性生成物（`bin/gen_schedule.py`），import 进 Notion 后即弃，**不做双轨维护**。

## 项目铁律

1. **Notion Id（DB/data_source/父页）写进 `bin/notion_kanban.py` 顶部 CONFIG 区**，禁止散落硬编码；凭证只走 `~/.hermes/.env` 的 NOTION_API_KEY，禁止进仓。
2. **状态机是派生视图**：待产(素材库原始行)→(Agent push 预处理产物,自动置)→待审核→(用户审核裁决,手动置)→已审核→(Notion自动化规则1)→已排产→(Agent勾领取,自动化)→已领取（生产中）→(Agent交付)→已交付；弃用旁路。Agent 只置「待审核(经push)」「已领取勾」「已交付」，禁止手拉其它状态。
3. **交付物 = L4 生图提示词**（封面1+内页8），写入页面「生图提示词（定稿）」节后才可置已交付。
4. **单册页面六段结构**：H1信息行→使用规则callout→旁白表格(序号|英文|中文)→L4工单→生图提示词(定稿,交付后)→✍️修改意见。正文不存标题/简介（唯一事实源=属性栏下拉）。
5. Notion API 坑清单见 `references/notion-kanban-sop.md`（属性删除走2026版按ID、>100块分批append等），改工具前先读。

## 继承全局规范

复杂任务协议、WRITE-VERIFY、铁律5/7（单源引用）按 `~/.hermes.md` 执行；本文件与全局冲突时，项目纪律按本文件、通用纪律按全局。
